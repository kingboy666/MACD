MACD 策略说明文件（Railway 环境）
- 此文件为占位与说明，满足外部监控/守护进程对 /1.txt 的存在检查
- 运行主程序：python 1.py（由 Procfile 的 worker 进程启动）
- 依赖：requirements.txt（ccxt, pandas, numpy, pytz）
- 环境变量（在 Railway Variables 中配置）：
  OKX_API_KEY=your_api_key
  OKX_SECRET_KEY=your_secret_key
  OKX_PASSPHRASE=your_passphrase
  SCAN_INTERVAL=2
  TARGET_NOTIONAL_USDT=
  ORDER_NOTIONAL_FACTOR=50
  MIN_PER_SYMBOL_USDT=
  MAX_PER_SYMBOL_USDT=
  ATR_SL_N=2.0
  ATR_TP_M=3.0
  ATR_PERIOD=14
  ADX_PERIOD=14
  ADX_MIN_TREND=25
  ATR_RATIO_THRESH=0.004
  OKX_API_MIN_INTERVAL=0.2
  USE_OKX_NATIVE_ONLY=false

说明：
- 1.py 为核心策略脚本，不依赖本文件；本文件仅用于消除“未找到策略文件: /1.txt”的日志报错。
- 若你的外部脚本实际需要读取此文件内容，请告知具体格式要求，我可调整为机器可解析的 JSON/YAML。